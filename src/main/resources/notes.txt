#We have centralized client , which we added during project creation. Service config client
#We will start with a simple hard coded rest api then we would enhance it to pick it up
from configuration and after that centralized configuration

leave this project here and , create a new project to setup spring cloud config server

Okay after the setup of config-server now if you do http://localhost:8080/limits
you will receive an response from the properties which is in local git repo and not configuration file

so far what we did was we connected limits ervice -> config server -> git repo